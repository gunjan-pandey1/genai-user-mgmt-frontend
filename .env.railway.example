# Frontend Environment Variables for Railway
# Copy these to Railway Dashboard → Your Service → Variables

# Backend API URL (get from your deployed backend service)
VITE_API_URL=https://your-backend-name.railway.app

# Port (automatically set by Railway, don't set manually)
# PORT=80
